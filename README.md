# Maps App - Приложение для поиска точек интереса и построения маршрутов

Мобильное приложение на Flutter для отображения карты, поиска ближайших точек интереса (POI) и построения маршрутов между локацией пользователя и выбранной точкой.

## 🚀 Возможности

- 🗺️ Интерактивная карта с поддержкой зума и жестов
- 📍 Отображение точек интереса (POI) с группировкой кластеров
- 🧭 Определение текущей геопозиции пользователя
- 🛣️ Построение реальных маршрутов по дорогам (через OSRM API)
- 📏 Расчет расстояния до выбранной точки
- 🎨 Современный Material Design 3 интерфейс
- 🌓 Поддержка светлой и темной темы
- ➕➖ Кнопки управления зумом карты
- 📱 Адаптивный дизайн для разных экранов

## 📋 Требования

Перед запуском убедитесь, что у вас установлено:

- **Flutter SDK**: версия 3.9.2 или выше
- **Dart**: версия, совместимая с Flutter SDK
- **Android Studio** (для Android) или **Xcode** (для iOS)
- **Git**

### Проверка установки Flutter

```bash
flutter --version
flutter doctor
```

Команда `flutter doctor` покажет, что необходимо установить или настроить.

## 🛠️ Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <URL_вашего_репозитория>
cd maps_app
```

### 2. Установка зависимостей

```bash
flutter pub get
```

Это установит все необходимые пакеты:
- `flutter_map` - библиотека карт
- `flutter_map_marker_cluster` - группировка маркеров
- `geolocator` - геолокация
- `flutter_bloc` - управление состоянием
- `http` - HTTP запросы для маршрутов
- `latlong2` - работа с координатами
- `equatable` - сравнение объектов

### 3. Настройка разрешений

#### Для Android

Откройте `android/app/src/main/AndroidManifest.xml` и убедитесь, что добавлены разрешения:

```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
<uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
```

#### Для iOS

Откройте `ios/Runner/Info.plist` и добавьте описания для разрешений:

```xml
<key>NSLocationWhenInUseUsageDescription</key>
<string>Приложению нужен доступ к вашей геопозиции для отображения на карте</string>
<key>NSLocationAlwaysUsageDescription</key>
<string>Приложению нужен доступ к вашей геопозиции</string>
```

### 4. Запуск приложения

#### Запуск на эмуляторе/симуляторе:

```bash
# Запустить эмулятор Android или симулятор iOS
# Затем выполнить:
flutter run
```

#### Запуск на физическом устройстве:

1. Подключите устройство к компьютеру через USB
2. Включите режим разработчика на устройстве
3. Проверьте подключение:
   ```bash
   flutter devices
   ```
4. Запустите приложение:
   ```bash
   flutter run
   ```

#### Выбор конкретного устройства:

```bash
flutter run -d <device_id>
```

### 5. Режимы сборки

#### Debug (разработка):
```bash
flutter run
```

#### Release (оптимизированная сборка):
```bash
flutter run --release
```

#### Profile (профилирование производительности):
```bash
flutter run --profile
```

## 📦 Сборка APK/IPA

### Android APK:
```bash
# Debug APK
flutter build apk --debug

# Release APK
flutter build apk --release
```

Готовый APK будет в: `build/app/outputs/flutter-apk/app-release.apk`

### Android App Bundle (для Google Play):
```bash
flutter build appbundle --release
```

### iOS (только на macOS):
```bash
flutter build ios --release
```

## 🗂️ Структура проекта

```
lib/
├── main.dart                          # Точка входа
├── app.dart                           # Конфигурация приложения
└── features/
    └── map/
        ├── data/                      # Слой данных
        │   ├── poi_loader.dart       # Загрузка POI из JSON
        │   ├── poi_repo.dart         # Репозиторий POI
        │   └── routing_service.dart  # Сервис построения маршрутов
        ├── domain/                    # Доменная логика
        │   └── poi.dart              # Модель POI
        └── presentation/              # UI слой
            ├── cubit/                # Управление состоянием
            │   ├── map_cubit.dart
            │   └── map_state.dart
            ├── pages/
            │   └── map_screen.dart   # Главный экран
            └── widgets/
                ├── map_widget.dart           # Виджет карты
                ├── poi_bottom_sheet.dart     # Нижняя панель с информацией
                └── zoom_controls.dart        # Кнопки зума

assets/
└── json/
    └── poi.json                       # Данные точек интереса
```

## 📝 Настройка точек интереса (POI)

Точки интереса хранятся в файле `assets/json/poi.json`. Формат:

```json
[
  {
    "id": 1,
    "name": "Название места",
    "lat": 43.238949,
    "lng": 76.889709
  }
]
```

**Чтобы добавить свои точки:**

1. Откройте `assets/json/poi.json`
2. Добавьте новые объекты с полями:
   - `id` - уникальный идентификатор
   - `name` - название точки
   - `lat` - широта (latitude)
   - `lng` - долгота (longitude)
3. Перезапустите приложение

**Как узнать координаты:**
- Откройте Google Maps
- Щелкните правой кнопкой на нужное место
- Скопируйте координаты (первое число - lat, второе - lng)

## 🔧 Решение проблем

### Проблема: "Location services are disabled"
**Решение:** Включите GPS/геолокацию на устройстве

### Проблема: "Location permission denied"
**Решение:**
1. Откройте настройки приложения на устройстве
2. Предоставьте разрешение на доступ к местоположению
3. Перезапустите приложение

### Проблема: Маршрут отображается прямой линией
**Решение:**
- Проверьте подключение к интернету (требуется для API маршрутов)
- API OSRM может быть временно недоступен - приложение автоматически покажет прямую линию

### Проблема: Карта не загружается
**Решение:**
- Проверьте интернет-соединение
- Убедитесь, что в AndroidManifest.xml есть разрешение INTERNET

### Проблема: Ошибки при flutter pub get
**Решение:**
```bash
flutter clean
flutter pub get
```

### Проблема: Приложение не собирается
**Решение:**
```bash
flutter clean
flutter pub get
flutter pub upgrade
flutter run
```

## 🌐 Используемые сервисы

- **OpenStreetMap** - тайлы карты (бесплатно)
- **OSRM (Open Source Routing Machine)** - построение маршрутов (бесплатно, без API ключа)

## 📱 Использование приложения

1. **Запустите приложение** - дайте разрешение на доступ к геолокации
2. **Ваша позиция** отобразится синим маркером с иконкой человека
3. **Точки интереса** показаны красными маркерами
4. **Выберите точку** - нажмите на любой красный маркер
5. **Информация о точке** появится в нижней панели
6. **Построить маршрут** - нажмите кнопку "Построить маршрут"
7. **Управление картой:**
   - ➕ Кнопка зума "+" увеличивает карту
   - ➖ Кнопка зума "-" уменьшает карту
   - 📍 Кнопка "My Location" центрирует карту на вашей позиции
   - Жесты: щипок для зума, перетаскивание для перемещения

## 🎨 Темная тема

Приложение автоматически переключается между светлой и темной темой в зависимости от системных настроек устройства.

## 📄 Лицензия

Проект создан в образовательных целях.

## 👨‍💻 Разработка

### Архитектура
Приложение использует **Clean Architecture** с разделением на слои:
- **Data** - работа с данными (API, JSON)
- **Domain** - бизнес-логика и модели
- **Presentation** - UI и управление состоянием (BLoC/Cubit)

### Паттерны
- **BLoC/Cubit** - управление состоянием
- **Repository** - абстракция работы с данными
- **Dependency Injection** - через конструкторы

### Запуск тестов
```bash
flutter test
```

### Анализ кода
```bash
flutter analyze
```

### Форматирование кода
```bash
dart format .
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте раздел "Решение проблем" выше
2. Выполните `flutter doctor` и устраните найденные проблемы
3. Убедитесь, что все зависимости установлены: `flutter pub get`

---

**Приятного использования! 🗺️✨**
